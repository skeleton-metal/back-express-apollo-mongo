type Role {
    id: _id!
    name: String!
    permissions: [String]
}

input RoleInput{
    name: String!
    permissions: [String]
}

type Query {
    roles: [Role]
    role(id:_id!): Role
}

type Mutation {
    createRole(input: RoleInput): Role
}